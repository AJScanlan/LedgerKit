\* Variant "guard" — see the table at the head of LedgerStore.tla.
\* Two senders: A3 needs one, but SingleFlight needs two to be a real claim.

CONSTANTS
    Senders = {"s1", "s2"}
    Fix = "guard"

SPECIFICATION Spec

INVARIANTS
    NoOrphanRows
    GenesisFirst
    SingleFlight
